# Backend API base URL (example)
VITE_API_BASE_URL=http://localhost:8080/api

# fetch timeout (ms)
VITE_API_TIMEOUT_MS=10000

# if true, when API fails the app falls back to local storage seed data
VITE_API_USE_LOCAL_FALLBACK=true
